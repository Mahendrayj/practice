
import java.util.Timer;
public class Date {
   Date currentDate=new Date();
   String formattedDate = String.format("Current Date: %tF", currentDate);
   {
   System.out.println(formattedDate);
   }
    
}
